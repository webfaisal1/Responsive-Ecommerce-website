import React, { Fragment } from 'react';
import Banner from '../Components/Banner';

const Home = () => {
  return (
    <Fragment>
      <section className='container' >
        <Banner />
      </section>
    </Fragment>
  );
};

export default Home;